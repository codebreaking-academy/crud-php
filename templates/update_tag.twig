{% extends "base.twig" %}
{% block title %}
	Modifier tag

{% endblock %}
{% block main %}
	<!-- Conteneur principal -->
	<div id="mainContainer">
		<div
			class="form">
			<!-- Contenu principal -->
			<main
				class="main-content">
				<!-- En-tête -->
				<h2 class="heading">
					<div>
						Mettre à jour le tag #


						{{tag.id}}
					</div>
				</h2>
				<form
					action="update_tag.php?id={{tag.id}}" method="post">
					<!-- Contenu -->
					<div
						class="content">
						<div
							id="addProductForm" class="product-form">
							<!-- Nom et type-->
							<div class="input-row">
								<div class="input-group">
									<div class="mb-3">
										<label for="tag">
											Nom Tag


										</label>
										<input type="text" name="tag" class="form-control" value="{{tag.tag}}" required/>
									</div>
								</div>
							</div>
							<input type="submit" value="Modifier" class="validate-button"></div>
					</div>
				</form>
			</br>
			<!-- Message de sortie -->
			{% if msg %}
				<h5>
					{{ msg }}
				</h5>
			{% endif %}
		</div>
	{% endblock %}
